% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_concentration.R
\docType{data}
\name{geom_concentration}
\alias{geom_concentration}
\alias{StatConcentration}
\alias{stat_concentration}
\title{Add a concentration curve layer to a ggplot}
\usage{
geom_concentration(
  mapping = NULL,
  data = NULL,
  stat = "concentration",
  position = "identity",
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE,
  rank_ascend = TRUE,
  round_rank = NULL,
  ...
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{aes()}. Must include
\code{x} (rank variable) and \code{y} (outcome variable).}

\item{data}{The data to be displayed in this layer.}

\item{stat}{The statistical transformation to use on the data (default: "concentration").}

\item{position}{Position adjustment (default: "identity").}

\item{na.rm}{If \code{FALSE} (default), missing values are removed with a warning.
If \code{TRUE}, missing values are removed silently.}

\item{show.legend}{Logical. Should this layer be included in the legends?}

\item{inherit.aes}{If \code{FALSE}, overrides the default aesthetics.}

\item{rank_ascend}{Logical. If \code{TRUE} (default), ranks from smallest to
largest values of x.}

\item{round_rank}{Numeric. Number of decimal places to round x before ordering
and grouping. If \code{NULL} (default), no rounding occurs.}

\item{...}{Other arguments passed on to \code{geom_line()}.}
}
\value{
A ggplot2 layer.
}
\description{
This geom creates a concentration curve directly from raw data, handling the
ranking and aggregation internally.
}
\examples{
library(ggplot2)
ggplot(food_dat, aes(x = INCOME, y = X_full)) +
  geom_concentration(rank_ascend = TRUE, round_rank = 0) +
  geom_abline(slope = 1, intercept = 0, linetype = 2) +
  coord_equal() +
  theme_minimal()
}
\keyword{datasets}
